
initial server side render
oauth with google, facebook?
email activation
404 not found page
api request 404 error catch







                {this.state.inputFields.map(inputField =>(
                  <div className="form-group left" key={inputField.id}>
                    <label htmlFor={inputField.id}>{inputField.name}</label>
                    <input id={inputField.id} className="form-control" type={inputField.type ? inputField.type : "text"} placeholder={"Enter "+inputField.name} autoFocus="" onChange={this.handleInputChange} />
                    {this.renderErrors(inputField.id)}
                  </div>
                ))}




                <div className="form-group left">
                  <label htmlFor="FirstName">First Name</label>
                  <input id="FirstName" className="form-control" placeholder="Enter First Name" autoFocus="" onChange={this.handleInputChange} />
                </div>

                <div className="form-group left">
                  <label htmlFor="LastName">Last Name</label>
                  <input id="LastName" className="form-control" placeholder="Enter Last Name" autoFocus="" onChange={this.handleInputChange} />
                </div>

                <div className="form-group left">
                  <label htmlFor="Email">Email address</label>
                  <input type="email" className="form-control" id="Email" aria-describedby="emailHelp" placeholder="Enter email" required onChange={this.handleInputChange} />
                  {this.renderErrors('Email')}
                </div>

                <div className="form-group left">
                  <label htmlFor="Password">Password</label>
                  <input type="password" id="Password" className="form-control" placeholder="Enter Password" required onChange={this.handleInputChange} />
                  {this.renderErrors('Password')}
                </div>

                <div className="form-group left">
                  <label htmlFor="PasswordConfirm">Confirm Password</label>
                  <input type="password" id="PasswordConfirm" className="form-control" placeholder="Enter Confirm Password" required onChange={this.handleInputChange} />
                  {this.renderErrors('PasswordConfirm')}
                </div>